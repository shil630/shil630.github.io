source "https://rubygems.org"

# 使用 GitHub Pages 官方 gem，保证与 GitHub 服务端构建环境一致
gem "github-pages", group: :jekyll_plugins

# 本地预览可选依赖
group :jekyll_plugins do
  gem "jekyll-feed"
  gem "jekyll-seo-tag"
  gem "jekyll-paginate"
  gem "jekyll-sitemap"
end

# Windows / 新版 Ruby 兼容
gem "webrick", "~> 1.8"
